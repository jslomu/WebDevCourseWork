function randomRange() {
  // Only change code below this line
  let myMin = parseInt(document.getElementById("myMin").value);
  let myMax = parseInt(document.getElementById("myMax").value);
  return 0;
  // Only change code above this line
}